<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Draw the overlay; the always-present sentinel also keeps the SAW->FGS
         background-start exemption valid under Android 16 (plan section 4.1). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Auto-start on boot (plan section 9). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Exact alarm for the recovery trigger. USE_EXACT_ALARM is auto-granted; the
         Play restriction to alarm/calendar apps is moot when sideloaded. Only exact
         alarms earn the exemption to start the FGS from the background. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Cleartext http:// is permitted for the poll endpoint and the WebView source
         (user decision); Android blocks cleartext by default since API 28. -->
    <application
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PuzzleAlerter">

        <!-- First-run configuration: capture and persist the poll URL, grant perms. -->
        <activity
            android:name=".ConfigActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- The poller + overlay owner. specialUse because no built-in FGS type fits
             "poll a URL"; the Play Console justification is moot when sideloaded. -->
        <service
            android:name=".PollService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Polls a personal URL and shows a full-screen reminder overlay"/>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Recovery alarm target. Explicit-intent, internal use only. -->
        <receiver
            android:name=".RecoveryReceiver"
            android:exported="false"/>

    </application>

</manifest>
